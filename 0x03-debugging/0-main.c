#include "main.h"

/**
* main - tests function that prints if integer is positive or negative
* Return: 0
*/

int main(void)
{
        int i;

        i = 0;   /* This is the only line you need to change */
        positive_or_negative(i);

        return (0);
}
